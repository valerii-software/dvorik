services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dvorik
      POSTGRES_PASSWORD: dvorik
      POSTGRES_DB: dvorik
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
